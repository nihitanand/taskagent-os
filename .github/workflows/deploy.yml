name: Deploy to Hostinger

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy index.html via curl FTP
        run: |
          curl -v --ftp-ssl \
            --user "u344800990.aldancare.com:${{ secrets.FTP_PASSWORD }}" \
            -T index.html \
            "ftp://77.37.37.73/domains/aldancare.com/public_html/tasks/index.html" \
            --ftp-pasv \
            --insecure 2>&1 | tail -20
