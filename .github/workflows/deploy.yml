name: Deploy to Hostinger

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy index.html via FTP
        run: |
          curl -v \
            --user "u344800990.aldancare.com:${{ secrets.FTP_PASSWORD }}" \
            -T index.html \
            "ftp://77.37.37.73/tasks/index.html" \
            --ftp-pasv 2>&1 | tail -15
